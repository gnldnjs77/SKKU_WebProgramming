<!DOCTYPE html>
<html>
	<head>
		<meta charset="utf-8">
		<meta http-equiv="X-UA-Compatible" content="IE=edge,chrome=1">
		<title>User</title>
		<link rel="stylesheet" type="text/css" href="/stylesheets/style.css">
	</head>
	<body>
		<div class="board-page">
			<div class="board">
				<h1>사용자 정보 수정</h1>
				<% if (status=='OK') {%>
					<% if(typeof data != 'undefined') { %>
						<% if (data.length == 0) {%>
							<div class="board-form">
								<p>존재하지 않는 사용자입니다.</p>
							</div>
						<% } else { %>
							<div class="board-form">
                                <input id="uid" type="hidden" value="<%=data[0].uid%>"/>
                                아이디: <input id="login_id" type="text" value="<%=data[0].login_id%>" disabled/>
                                비밀번호: <input id="login_pwd" type="text" value="<%=data[0].login_pwd%>"/>
                                이름: <input id="user_name" type="text" value="<%=data[0].user_name%>"/>
                                이메일: <input id="email" type="text" value="<%=data[0].email%>"/>
							</div>
							<div align="right">
                                <button id="btn_user_update">사용자 정보 수정</button>
                                <button id="btn_user_delete">사용자 삭제</button>
								<button id="btn_user_list">사용자 목록</button>
							</div>
						<% } %>
					<% } else {%> <!--data가 undefined-->
						<div class="board-form">
							<p>사용자 정보를 불러오는 중 오류가 발생했습니다.</p>
						</div>
					<% } %>
				<% } else { %>
					<div class="board-form">
						<p>사용자 정보를 불러오는 중 오류가 발생했습니다.</p>
					</div>
				<% } %>
			</div>
		</div>
		<script src="https://code.jquery.com/jquery-3.4.1.min.js"></script>
        <script src="/javascripts/users.js"></script>
	</body>
</html>